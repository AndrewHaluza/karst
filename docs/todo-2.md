- need to mechanizm to auto update settings if user manually changed karst.yml; but need to dump before, and have validation; user might break the config;
- should be available button to manual resync settings from karst.yml to actual settings; on the settings general page

- services has a bug with expand of service; lable took whole width and expand is not clickable there; we should have expand button/area only on the right side of the label;
- name of service unable to edit; 

- on create ticket page:
    - "Edit ticket #4" sql id should be replacedwith ticket number and title, not an internal id; if no id - it's create ticket mode;
    - only "Recommend" word should be in the bubble; explanation should be after ":"
    - suggest button should be inactive if no ticket is fetched, or it's already did recommend; 
- tiket list redesign;
- tickets should has states;
- we should able to filter tickets by predefined states;
- should be able to remove tickets(soft and hard delete);
- archive it's soft deleted items; need a button to unarchive;